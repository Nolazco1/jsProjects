let computer_parts = ["Monitors", "Motherboards", "Chips", "Hard Drives", "DVD-ROMs", "Cases", "Power Supplies"];
for (let parts = 0; parts < computer_parts.length; parts++) {
    document.write(computer_parts[parts] + "<br>");
}